version: 0.1

phases:
  install:
    runtime-versions:
        python: 3.7
  build:
    commands:
      - echo nothing to do yet
artifacts:
  files:
    - index.html
    - favicon.ico
    - styles/main.css
    - images/*
