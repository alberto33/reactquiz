version: 0.2

phases:
  install:
    runtime-versions:
        python: 3.7
  build:
    commands:
      - echo nothing to do yet
artifacts:
  files:
    - index.html
    - favicon.ico
    - styles/main.css
    - images/*
